venv/
*.pyc
.env
.env.backup
.DS_Store
keys.json
sourcing.db
domain_cache.json
roster.json
blacklist.json
local_errors.log


# Backend railway up build-config override — must NEVER be committed (would hijack the Streamlit service build)
/railway.json
